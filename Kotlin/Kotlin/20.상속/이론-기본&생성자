상속(Inheritance)
상속이 필요한 경우
    -확장된 클래스를 만들고 싶은 경우
        -class Warrior(){ -> 바탕이 되는 클래스
            fun attack(){}
        }
        -class DefenseWarrior(){ -> 확장된 클래스
            fun attack(){}
            fun defense(){}
        }
        -class HardAttackWarrior(){ -> 확장된 클래스
            fun attack(){}
            fun defense(){}
        }
    -이미 존재하는 클래스를 합칠 때
-상속은 공통점 찾기가 아니다!!!!

-키워드
    -open 키워드로 설정된 클래스만 상속 가능
    -open 키워드는 class 앞에 적어줌
    -final로 설정된 클래스는 상속 불가 -> 기본
-슈퍼클래스/서브클래스, 부모클래스/자식클래스
    -서브클래스는 슈퍼클래스의 생성을 책임져야 함
    -슈퍼클래스가 사용 중인 멤버와 동일한 이름의 멤버 만들기 불가
    -자식클래스는 부모클래스가 가지고 있는 모든 것을 물려받음
    -상속은 여러 번 가능
    -다중상속 불가(A, B를 상속(다중상속))하는 클래스 C는 불가